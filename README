To run the notebook, activate your conda sotodlib environment beforehand.

Modified context writer (adapted from SO) can be found in `context_generator.py`.

For map visualization, astropy package is required.
